default:
  outputs:
    local:
      type: postgres
      threads: 4
      host: "localhost"
      port: 5432
      user: "postgres"
      pass: "test3"
      dbname: "priceloop"
      schema: amazon

  target: local